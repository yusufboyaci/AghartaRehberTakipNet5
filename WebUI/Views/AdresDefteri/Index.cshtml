@model IEnumerable<AdresDefteriVM>

@{
    ViewData["Title"] = "Index";
    List<KisiVM> kisiler = ViewBag.kisiler;
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Id
            </th>
            <th>
                Adres
            </th>
            <th>
                Mail
            </th>
            <th>
                Konum
            </th>
            <th>
                Kisi
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
      
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Adres)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Mail)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Konum)
                    </td>
                    <td>
                        @*@kisiler.FirstOrDefault(x => x.Id == item.KisiId).Ad @kisiler.FirstOrDefault(x => x.Id == item.KisiId).Soyad*@
                    </td>
                    <td>

                        <a asp-controller="AdresDefteri" asp-action="Edit" asp-route-id="@item.Id">Edit</a>|
                        <a asp-controller="AdresDefteri" asp-action="Delete" asp-route-id="@item.Id">Delete</a>

                    </td>
                </tr>
            }
        
    </tbody>
</table>
